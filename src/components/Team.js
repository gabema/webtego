import React from 'react';
import PropTypes from 'prop-types';
import './Team.css';

const Team = ({color, pieces}) => (
  <div className="Team">
  </div>
);

Team.PropTypes = {
    pieces: PropTypes.array,
};

export default Team;
